core:
  includes:
    - components/vs/visual-studio
    - components/nuget/core

library:
  includes:
    - stacks/dotnet/core

web:
  includes:
    - stacks/dotnet/core
  chef:
    json:
      gusztavvargadr_workstations_core:
        tools:
          chocolatey:
            postman:
            fiddler:
